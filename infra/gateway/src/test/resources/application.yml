spring:
  cloud:
    config:
      enabled: false
  config:
    import: ""
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:41880/realms/novamarket

eureka:
  client:
    enabled: false
